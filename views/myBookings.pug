extends layout


block content
  h1 My Bookings
  //#{title}
  ul.list-group
    each booking, i in bookings
        if user.id == booking.author
            li.list-group-item
                a(href="/bookings/show/"+booking._id)= booking.title              